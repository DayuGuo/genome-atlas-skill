# Example output

`report.html` is the report produced from a real 30× whole-genome delivery for one East Asian sample,
published with that person's consent. The name, sex and test date have been removed. `report_text.yaml`
is the copy behind it.

**Use both for format and tone only.** Every conclusion in them belongs to that sample. Copy the yaml to
`work/report_text.yaml` and rewrite every line before you deliver anything.
